# name: Build and Push Simple API Server

# on:
#   push:
#     branches:
#       - main
#       - master
#       - develop
#     paths:
#       - 'apps/simple-api-server/**'
#       - '.github/workflows/build-simple-api-server.yml'
#   pull_request:
#     branches:
#       - main
#       - master
#       - develop
#     paths:
#       - 'apps/simple-api-server/**'
#       - '.github/workflows/build-simple-api-server.yml'
#   workflow_dispatch:
#     inputs:
#       tag:
#         description: 'Image tag (default: latest)'
#         required: false
#         default: 'latest'

# env:
#   REGISTRY: ghcr.io
#   IMAGE_NAME: ${{ github.repository }}/simple-api-server

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to GitHub Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Extract metadata (tags, labels) for Docker
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
#           tags: |
#             type=ref,event=branch
#             type=ref,event=pr
#             type=semver,pattern={{version}}
#             type=semver,pattern={{major}}.{{minor}}
#             type=sha,prefix={{branch}}-
#             type=raw,value=latest,enable={{is_default_branch}}

#       - name: Build and push Docker image
#         uses: docker/build-push-action@v5
#         with:
#           context: ./apps/simple-api-server
#           file: ./apps/simple-api-server/Dockerfile
#           push: ${{ github.event_name != 'pull_request' }}
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#       - name: Image digest
#         run: echo "Image pushed with digest ${{ steps.meta.outputs.digest }}"
